Hello <%= @user.name_or_email %>,

To access <%= ENV['APP_NAME'] %> just use the following authentication code:

  <%= @password %>

This is a random code and it is only valid for a few minutos. You don't have to memorize it.
