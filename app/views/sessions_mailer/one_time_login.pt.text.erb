Olá <%= @user.name_or_email %>,

Para acessar o <%= ENV['APP_NAME'].dup.force_encoding('UTF-8') %> basta usar o seguinte código de acesso:

  <%= @password %>

Este código foi gerado aleatoriamente e é válido somente por alguns minutos. Você não precisa memorizá-lo.
